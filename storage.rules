rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Files are stored under user UID folders, e.g. /<uid>/audio.mp3
    match /{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
